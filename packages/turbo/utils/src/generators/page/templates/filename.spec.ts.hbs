import { expect, test } from "@playwright/test";

test("has expected title", async ({ page }) => {
  await page.goto("{{ route }}");

  const Title = await page.title();

  expect(Title).toBe("{{ pascalCase name }}");
});
